param_3 = "A global var"

fn my_func: param_1, param_2
  println(param_1)
  println(param_2)
end

my_func("param_1", "param_2")
param_1 # Should raise an error, since param_1 does not exist in the global scope.
